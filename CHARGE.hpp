#ifndef CHARGE_H
#define CHARGE_H

#include <vector>
#include <iostream>
#include <cstdlib>

#define MAX_CHARGE_VALUE 100
#define MIN_CHARGE_VALUE 0

using namespace std;

vector<int> generateChargeValues(int maxValue, int minValue, int readingCount);
#endif
